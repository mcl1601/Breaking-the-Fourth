using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace BreakingTheFourth
{
    class Level
    {
        //Here we have the parent level class.
        //Not even gonna make the child classes until we know exactly what we're doing with this one.
        //Do we make the LEVEL a Rectangle? Probably not. But then how do we fit the textures in? 
        //Collision Detection. It's a thing.
        //We'll need to read in the files with the level format. Same with the textures. May be done in Config
        //Level Start and Level End methods.
        //Constructor?
    }
}
